name: Android-Build-Sonic
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  check-system:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: echo "El motor de GitHub Actions ha despertado"
